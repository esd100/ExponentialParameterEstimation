# Changelog — charter and gaps register

Running log, newest first. Each entry says what changed in the living documents and why. Companion docs: `charter.md`, `gaps-register.md`, `claude/phase0-findings.md`, `claude/phase0-harness-status.md`.

---

## 2026-09-10 — charter v0.14 → v0.15 · bibliography v0.5 → v0.6 · tissue dictionary v2.2 → v2.3 · `literature-requests.md` v4 → v5 · gaps register v0.9 → v0.10 · harness status rev 7 → 8 · findings rev 6 addendum

**Trigger:** Eric's session brief — read the ten unread water–fat papers applying the findings already logged for the other five (Hu 2011 no multi-organ table, Reeder 2011, Bydder 2008 Table 2, Armstrong 2018, the Tang 2013/2015 origin-vs-validation correction); dictionary schema 2.3 with `condition`, `clinical_delta` and the five pathology rows; the threshold re-run with the SD ≤ |Δ|/3 label; the CRLB map over the dictionary; charter v0.15; commit, tag, push. The five findings named in the brief were not in any project document or the repository, so all fifteen papers were read from `literature/` this session and the five findings confirmed against the PDFs before being applied.

**The water–fat pass (bibliography v0.6).** All fifteen `CHECKED-PRIMARY` with the numbers taken per row. What moved: *liver PDFF* → PRIMARY on Szczepaniak 2005 (Dallas Heart Study, 1.5 T PRESS TR 3 s / TE 25 ms, HTGC = methylene / (methylene + water) with fixed T2 correction; low-risk subgroup n = 345 median 1.9 %, 90th / 95th percentile 4.3 / 5.56 %; cohort median 4.69 %, 5th–95th 0.99–22.86 %); *pancreas* → PRIMARY on Kühn 2015 (SHIP, n = 1,241, mean 4.4 %, head / body / tail 4.6 / 4.9 / 3.9 %, no dependence on glucose tolerance) with Idilman 2015 (5.7 % in NAFLD, 12.2 % in diabetics); *renal cortex* → PRIMARY on Idilman 2015 (1.7 ± 1.4 %, median 1.3, range 0.1–7.6; medulla as cortex, THEORY); *adipose* → SECONDARY on Guglielmo 2023 (93–96 %); marrow keeps Le Ster 2016 with Idilman's 43 % in an older NAFLD cohort noted. *Liver water / fat T2*: Bydder 2008 Table II (1.5 T STEAM, TE 20–70 ms, 21 fatty livers) water 36.2 ± 4.9 / CH2 74.8 ± 19.2 ms; Hamilton 2011 Table 3 (3 T STEAM) water 23 / CH2 62 ms, plus the six-peak spectrum (70.0 % at 1.3 ppm, 12.0 at 2.1, 8.8 at 0.9, 4.7 at 5.3, 3.9 at 4.2, 0.6 at 2.75; 8.6 % under the water peak). *Grade calibration*: Tang 2015 Table 3 validates 6.4 / 17.4 / 22.1 % (sensitivity 86 / 64 / 71 %, specificity 83 / 96 / 92 %; cohort-derived 6.9 / 16.4 / 23.5 %); Schwimmer 2015 Table 1 gives per-grade means 2.6 / 9.2 / 15.1 / 26.8 % in 174 children; Guglielmo 2023 Table 4 rounds to < 6 / 6–17 / 17–22 / > 22 % and Table 6 gives the liver R2* iron grades (normal < 75 s⁻¹ at 1.5 T, < 136 at 3 T). *Method papers*: Reeder 2011's confounder taxonomy (Table 2); Yu 2008's precalibrated six-peak amplitudes and the R2* overestimation of a single-peak model; Liu 2007's T1 bias (4 % at 5°, 29–45 % over 5–30°) and noise-floor bias (4–15 % → < 1 % by magnitude discrimination); Hernando 2012's mixed magnitude/complex fitting (a fourth data representation, noted in charter §2.3); Yokoo 2011's slope 0.982 / intercept −0.0007 against MRS at 3 T; Armstrong 2018's radial pipeline with limits of agreement ± 5 % vs Cartesian and ± 10 % vs MRS.

**Two attributions corrected, one expectation not met.** (1) The 6.4 / 17.4 / 22.1 % thresholds **originate in Tang 2013** (Radiology 267:422, NASH CRN; Tang 2015 cites it as reference 46 for them) and Tang 2015 is the independent-cohort *validation* — `literature-requests.md` #66's 'why' column and the bibliography's open note are corrected; Tang 2013 is listed as an optional request (#75). (2) **Schwimmer 2015 proposes no grade cut-offs**: the 5.3 / 14.4 / 22.1 % quoted for it in v4 (#67) are not in the paper, which tests four *published* grade-0-vs-1 thresholds (1.8 / 5.5 / 6.4 / 9.0 %) and reports that none is diagnostic for an individual child. (3) **Hu 2011 carries no multi-organ fat table** (Table 1 methods, Table 2 segmentation reproducibility), so muscle, myocardium and spleen PDFF stay RECALLED and **Grimm 2018 and Szczepaniak 2003 are re-requested** (#73–#74, DOIs confirmed against Crossref). Also: Yokoo 2011 reports no r² (the v4 gloss was a paraphrase).

**Dictionary v2.3 — the pathology axis.** Schema: `condition` ({name, kind ∈ normal / pathology / physiological, base, operating_point}) and `clinical_delta` ({functional, normal_value, disease_value, delta, kind, status, source, note} or None) on every entry; `variant()` builds disease rows that inherit the base row's bulk properties. Five rows: **steatotic liver at PDFF 10 % (grade 1) and 25 % (grade 3)** — water 1 − PDFF at 36.2 ms, fat PDFF at 74.8 ms (Bydder 2008, PRIMARY), the fractions as operating points inside Tang 2013's bands, the normal row's THEORY vascular tail dropped because the STEAM primaries see one water peak, functional 'fat fraction (1 − mass below 55 ms)', declared like the adipose row a misspecification row (J-coupled, chemically shifted); **malignant prostate PZ** (Sabouri 2017 Table 2: 0.90 at 81 / 0.10 at 548 ms, 64 × 25 ms, PRIMARY); **venous-filled skeletal muscle** (Araujo 2014 Table 1: 0.858 at 32.6 / 0.142 at 181 ms, 3 T, PRIMARY — the oedema surrogate); **MS-lesion white matter** (MacKay 1994: MWF 4.1–6.4 % by patient, mean 5.25 %, PRIMARY; IE T2 100 ms THEORY because MacKay plots but does not tabulate it; CSF-like 0.02 THEORY as the normal row). Δ on the normal rows where a primary gives the change: WM 0.113 → 0.0525 (MacKay lesions vs Whittall normal; −0.10 within MacKay), cord 0.296 → 0.2655 (age, MacMillan Table 3), muscle 0.080 → 0.142 (Araujo paired states), prostate 0.24 → 0.10 (Sabouri). Counts: 24 entries, 67 component values — 32 PRIMARY / 17 SECONDARY / 1 TERTIARY / 1 RECALLED / 16 THEORY. JSON schema 2.3 and the document regenerated (980 lines); `tests/test_tissues.py` gains four tests (base inheritance, Δ consistency and the four measured values, the steatotic fraction sweep, the amplitude-vector-at-fixed-ratio property).

**Threshold re-run with the third label (`results/threshold_tissues.md`).** 'Clinically estimable' = SD(functional) ≤ |Δ|/3. Reading: the prostate-cancer row at Sabouri's train fails the 10 % label (12 % relative) and passes the clinical one (SD 0.012 ≤ 0.047) — the label answers a different question; both prostate rows fail all three at the reference train. Myelin: |Δ|/3 = 0.020 against bounds of 0.074 / 0.057 (free) and 0.030–0.038 (constrained) at SNR 100, 0.010–0.013 at SNR 300 — detecting demyelination needs SNR ~300 with the long component constrained. Muscle: the venous-filled row is K-estimable, misses 10 % (17 %) and misses |Δ|/3 = 0.021 by a hair (0.023). Steatosis: fat-fraction bound 0.34 / 0.29 at the reference train, > 1 at the 16 × 8 ms abdominal train — fails as it should. No v0.14 label reversed by the new one.

**CRLB map (`scripts/phase0_crlb_map.py`, `results/crlb_map.md`, `.csv`, `tests/test_crlb_map.py`).** Per T2 row, free and with a > 1 s component fixed: SNR crossings of the three labels on the 32 × 10 ms train (grid 20–3000, log–log interpolation), echo-count crossings at SNR 100 (8–256 × 10 ms), and the functional's bound in the Gaussian-real and Rician arms with σ known or joint. Findings written into charter §1.2 and §8: myelin is an SNR axis (clinical label at SNR ≈ 360 free / 190 constrained; no N at SNR 100 — the bound saturates at 0.031–0.033 above N ≈ 128), the prostate a window axis (N = 48 at SNR 100; SNR ≈ 220 / 160 at N = 32 for normal / cancer); the venous-filled muscle crosses at SNR ≈ 110 / N = 48; the steatotic rows only at SNR ≈ 900 / 340 and never by window, and they are the only rows where the magnitude arm (16–37 %) and joint σ (6–10 %) cost anything — §3.2's degeneracy where the water signal reaches the floor inside the window, and nowhere else (charter §3.2 note). The complex arm is not tabulated: for a real kernel it equals the real arm. An indexing error in the first run (0-based `T2k` labels read as 1-based, so the constrained rows fixed the wrong component) was caught by comparing with the threshold table's 0.038 and is locked by a test.

**Charter v0.15.** Header; §1.2 (Tang attribution in the label justification; the third label and the map defined where v0.14 promised them; a pathology-axis-and-map paragraph after the bold per-tissue sentence); §2.3 (Hernando's mixed representation as a level of the data-representation axis); §3.2 (the map's σ-cost finding); §4 dictionary bullet (v2.3); §7 (water–fat pass done, what remains); §8 three rows; §10 step 5 (map built) and step 8 (a, c, e updated). **Harness:** 80 tests (~70 s); `README`s and `CITATION.cff` (charter-v0.15) updated.

**Not done, deliberately.** No estimator (G2); no noise realisation (neither script draws one). No figure for the map — it will be drawn beside the estimator results it is meant to bound. Iron-loaded liver, fatty pancreas / muscle / myocardium, degenerated cartilage, marrow infiltration and cord lesions are not rows yet: each waits on a primary (§10 step 8c). Tang 2013 itself was not opened (optional request #75); the threshold number reaches the harness through Tang 2015's validation.

---

## 2026-09-09 (evening) — charter v0.13 → v0.14 · bibliography v0.4 → v0.5 · `literature-requests.md` v3 → v4 · harness status rev 6 → 7

**Trigger:** Eric's three questions after the verification pass — where "recoverable with clinically useful precision" came from; the water–fat papers now retrieved (his own curated list of fifteen, replacing the nine I had requested); the `Claude outputs/` folder to be reduced to one copy of each living document.

**§1.2 — the estimability labels defined.** The table `results/threshold_tissues.md` has been quoted in the charter since v0.10 without the charter saying what 'K estimable' and 'functional estimable' mean. They are: every visible T2 under 25 % relative SD (Rician CRLB, σ known), and the clinical functional under 10 % relative SD (delta method). The numbers are the bounds of the v0.7 falsification clauses reused as row labels; "clinically useful precision" was my gloss in conversation, not a charter phrase. The justification now written into §1.2: 25 % on T2 is σ(ln T) ≈ 0.25, about a quarter of the log-resolution cell ln δ ≈ 0.9–1.0 at SNR 100, so the 2σ interval fits inside half the spacing to the nearest resolvable neighbour — the component is individually located rather than smeared; 10 % on the functional turns a 30 % change into a 3σ single-measurement event, and the normal-to-disease changes on record (MWF 11–16 % → 4–6 % in MS lesions, LWF 0.24 → 0.10 in cancer, PDFF grade steps of 6–11 points) are that large or larger; the field's own criteria are 10–15 % (Bouhrara 2015, Saab 1999). Both remain conventions at one operating point; the CRLB map turns them into crossing curves and the pathology axis adds the per-tissue criterion SD ≤ |Δ|/3. §8 row added.

**Water–fat reading list.** Eric replaced the v3 priority-1b block with a curated list of fifteen retrieved papers (`literature-requests.md` v4, #58–#72; his `literature-requests-1-updated.md` adopted as the living copy with a version paragraph added): Szczepaniak 2005, Reeder 2011 (for Reeder 2012), Hamilton 2011, Bydder 2008, Yu 2008, Liu 2007, Hernando 2012, Yokoo 2011 (for Yokoo 2018), Tang 2015 (for Tang 2013), Schwimmer 2015, Idilman 2015, Kühn 2015, Guglielmo 2023, Armstrong 2018, Hu 2011; Grimm 2018, Szczepaniak 2003 and Henkelman 1992 dropped unless Hu 2011 / Idilman 2015 leave muscle or myocardium unsupported. Bibliography v0.5 carries them `BIBLIO-ONLY (on hand)`; the Tang 2013 row keeps its RECALLED claim with a note that the 6.4 / 17.4 / 22.1 % attribution must be settled when Tang 2015 is read. Charter §7 and §10 step 8 updated: the PDFF ranges and the steatotic-liver rows are the next session's first reads, from primaries, no flag needed.

**`Claude outputs/`.** Reduced to one current copy of each living document (charter, gaps register, bibliography, changelog, phase0 findings and status, literature requests, tissue dictionary); the superseded copies and the delivered threshold table removed (results live in `mexp-harness/results/`). Delivered files land there with `-1` suffixes when names collide, so living documents will no longer be delivered to the chat by default — the repository and the project are where they live.

---

## 2026-09-09 (later) — charter v0.12 → v0.13 · bibliography v0.3 → v0.4 · tissue dictionary v2.1 → v2.2 · `literature-requests.md` v2 → v3 · harness status rev 5 → 6 · findings rev 5 addendum

**Trigger:** the dictionary verification pass (charter §10 step 8a), unblocked by the 57 PDFs in `literature/`; Eric's prompt fixed the reading order (Sabouri 2017 ×2 first, then Whittall 1997, MacKay 1994, Saab 1999, Araujo 2014, Stanisz 2005, MacMillan 2011, Labadie 2014, Prasloski 2012, Du 2012, Bouhrara 2015), the rules (G2: no estimator; §4: no noise by formula; primaries correct the charter, not the reverse) and the end state (commit, tag, project write-back). GitHub mirror `esd100/ExponentialParameterEstimation` created and pushed by Eric before the session (tags charter-v0.10–v0.12).

**Dictionary v2.2 — what the twelve papers changed.** All twelve read from `literature/` (text extracted in the cloud session; the tables quoted per row in `bibliography.md`). *Prostate* (Sabouri Radiology Table 2, JMRI Table 3): the last RECALLED component set → PRIMARY, and every recalled number was off — luminal water fraction 0.24 ± 0.09 (not 0.35), T2short 90 ± 26 / T2long 545 ± 115 ms (ratio ≈ 6, not 8), protocol 64 echoes × **25 ms** at SNR ~103 (not 64 × 8 ms at 80); the malignant-PZ numbers (LWF 0.10 at 81 / 548 ms) are recorded as the pathology row for the organ. *Brain* (Whittall 1997 Tables 1–3; MacKay 1994): WM myelin water 11.3 % (structure average, 8.4–15.0; MacKay's 15.6 ± 8.1 % kept as the range top), IE gmT2 77 ms; GM 3.1 % and 80 ms; water contents 0.708 / 0.832 g/ml concordant with Woodard & White; the CSF-like partial-volume components relabelled THEORY because both primaries see the > 1 s peak but exclude it from the tissue water without tabulating a fraction. *Cord* (MacMillan 2011 Tables 1, 3, 4): MWF 0.296 (dorsal 0.306, lateral 0.284; older adults 0.257–0.274), IE gmT2 100 ms (96.5–105.5), global gmT2 65–70 ms — the IE component moves from 75 to 100 ms. *Muscle* (Saab 1999 Table 1): 10.9 / 27.8 / 45.5 / 11.3 % at < 5 / 20.8 / 38.9 / 114.3 ms at **1.89 T** — the v2 "5 %" on the 114 ms pool was the fifth component (283 ms, 6 of 13 subjects) mis-assigned from the abstract; Araujo 2014 (3 T, ISIS-CPMG, fat-saturated) sees two components, 92 / 8 % at 32 / 159 ms, and its three-site exchange model makes the long one vascular water — recorded in the note, its venous-filling state (14 % at 181 ms) reserved as the oedema surrogate for the pathology axis. *WM T1* (Labadie 2014 Table 2): 0.083 at 225 ms with TIn = 17 s (0.050 at 113 ms with 8 s — the fraction and position depend on the TI range), T1,long 1.06 s, Look-Locker geometric TIs. *Tendon* (Du 2012 Table 1): 71.4 % at 1.28 ms + 28.6 % at 17.65 ms (bovine Achilles) — the v2 numbers were the goat-ligament row; Du's cartilage (0.48 ms 18.5 % + 35 ms) and cortical bone (0.29 ms 78 % + 2.93 ms) added to those notes. *Cartilage*: Bouhrara 2015 (bovine nasal cartilage, MGE, Rician-correct fit: biexponential in 20–26 % of voxels, ~0.07 at ~4–5 ms + ~60–67 ms) in the note. *Stanisz 2005*: Table 1 re-read from the publisher PDF, every value the mirror gave confirmed, all St05 values → PRIMARY. *Prasloski 2012*: no dictionary number, but its simulated WM truth (15 / 75 / 10 % at 20 / 100 / 2000 ms) and its finding that exponential analysis loses the myelin water below ~165° refocusing are recorded (WM note; bibliography). Status counts 7 / 31 / 1 / 6 / 11 → **23 / 17 / 1 / 1 / 14** (PRIMARY / SECONDARY / TERTIARY / RECALLED / THEORY); the one RECALLED value left is the renal-medulla IVIM D*. Schema 2.2; JSON and document regenerated; 71 tests (a new one asserts no RECALLED component set remains and that the prostate protocol is 64 × 25 ms).

**Threshold re-run — two class changes, logged in §8.** (1) Prostate at the *reference* 32 × 10 ms train: 'K estimable' → not (long T2 65 % relative, LWF 0.24 ± 0.105); at its own 64 × 25 ms train still ~5 % on the LWF. The 545 ms component is unpinned by a 320 ms window — the window rule of §1.2 in a clinical protocol, and the reason luminal water imaging uses a 1600 ms train. (2) Cervical cord under the K = 2 model at SNR 100: not estimable → estimable on both criteria (myelin T2 18 %, MWF ± 0.024). WM: MWF bound 0.074 / 0.038 / 0.030 (free / long fixed / dropped) for MWF 0.113, myelin T2 60 % in the K = 2 model — class unchanged (marginal). GM and muscle unchanged in class; the muscle long-T2 bound improves (249 → 106 %) with the larger extracellular fraction.

**Charter v0.13.** Header; §1.2 tissue-level paragraph (counts; the prostate numbers, ratio and train corrected with the v0.10–v0.12 error stated; cord added; the bold per-tissue sentence now names the window condition); §4 dictionary bullet (v2.2); §7 provenance paragraph (priority-1 pass done; what remains); §8 two rows; §10 step 8 (a) done, remaining reads listed, (e) to be built from Tang / Yokoo / Hamilton with a flag until they are on hand.

**PDFF ranges — not moved, and why.** None of the 57 PDFs is a water–fat paper for a lean organ (the closest, Le Ster 2016, is marrow and was applied in v2.1), so the RECALLED PDFF ranges for muscle, myocardium, liver, spleen, kidney, pancreas and breast stay RECALLED; `literature-requests.md` v3 adds a priority-1b block (#58–#66: Szczepaniak 2005, Tang 2013, Reeder 2012, Yokoo 2018, Hamilton 2011, Kühn 2015, Grimm 2018, Szczepaniak 2003, Henkelman 1992; DOIs confirmed against Crossref except where the row says otherwise) and the bibliography carries the new rows `BIBLIO-ONLY`.

**Not done, deliberately.** No estimator (G2); no noise realisation (the threshold script draws none). A second muscle T2 set at 3 T (Araujo) was not added because the schema keys one component set per modality per entry — the pathology axis will add the `condition` field that variant needs. The priority-2 bulk-property sources (T1 at 3 T, relative PD) are the next reading list after the pathology axis.

---

## 2026-09-09 — charter v0.11 → v0.12 · gaps register v0.7 → v0.8 · bibliography v0.2 → v0.3 · tissue dictionary v2 → v2.1 · `literature-requests.md` gains a retrieval-status column · licenses chosen

**Trigger:** Eric placed all 57 requested publications in `literature/` (2026-09-08) with an updated `literature-requests-updated.md`; asked for adipose tissue and per-organ fat content in the dictionary; accepted the license recommendation; asked for a GitHub repository; agreed that the abdominal multi-component measurements are a future research project, not current focus.

**§1.2 / §7 — the last provenance flag closed, and the constant corrected.** Istratov & Vyvenko 1999 was read from the PDF. Their SNR is an amplitude ratio (the formula carries SNR²; "exp(4.6) ≈ 100" fixes the decay-to-noise time at SNR 100; averaging gains √K), so the v0.11 resolution-in-principle stands. What the primary corrects is the constant: eqs (12)–(13) read δ = exp(π/ω_max) with cosh(πω_max) = **π·SNR²**, not SNR² — the factor is |Γ(½)|², the squared leading singular value of the Laplace transform, because their criterion is the absolute singular value against 1/SNR. Their Table I (2.44 / 1.88 / 1.63 at SNR 10²/10³/10⁴, plus the known-support columns 1.74/1.45/1.32 and 1.44/1.27/1.20) is now reproduced by the harness: the unrestricted column from the closed form exactly, the finite-domain columns from the harness's own SVD of a finite T-grid to the integer-count granularity. §1.2 states the printed form as the cited one and the harness's relative form (SNR² inside the arccosh, 2.71 at SNR 100) as what the numerics test; the two differ by ln π ≈ 1.14 in the denominator — 10 % in ratio at SNR 100, 0.4–0.8 in count, inside the ±1.5 already stated. `conditioning.r_min` and `k_max` carry the three conventions ("istratov", "arccosh", "charter"); `ISTRATOV_TABLE_I` holds the table; two tests lock it. Open §7 flags: none. The Steinbeck & Chmelka (2005) restatement cited in v0.11 turns out to quote Istratov & Vyvenko's literature review (Clayden's 2.4 / 3.5), not their Table I — still consistent only with the amplitude reading.

**§8 / §10 — Varah 1985 read.** The journal text, like the 1982 tech report, does **not** quote Lanczos's 2.202/4.45/0.305/1.58; Istratov & Vyvenko's Fig. 2 caption does, exactly. Varah's §2 gives the generator, Δt = 0.05, 24 points "truncating to two decimal places" (his Table 1 equals Lanczos's table at every point), and his Table 2 gives his own best two-term fit to it — a = (0.40, 2.11), b = (−1.81, −4.57), I = 1.0e-4 — which the harness's least squares reproduces (0.403/2.105, 1.809/4.572, SS 1.14e-4; new test `test_varah_1985_table_2_reproduces`). His Table 3 points (rate pairs 30 % apart) fit the two-decimal data within its own 0.005 precision. The v0.7 attribution "via Istratov & Vyvenko (1999) and Varah (1985)" is corrected in the charter and the bibliography; no locked test changes.

**Tissue dictionary v2.1 (19 entries).** (1) *Adipose tissue* added: composition from Woodard & White 1986 Table I (water 11–31 %, lipid 61–87 % by mass; composition 2 = 21.2/74.1 taken as the value), T1 288/371 ms and T2 165/133 ms at 1.5/3 T from Gold 2004's tables, with de Bazelaire 2004's 343/382 and 58/68 ms alongside and Bojorquez 2017's Table 2(a) spread of fat T2 at 3 T (41 ms single SE to 154 ms 16-echo CPMG to 371 ms hybrid SE) recorded as *sequence dependence*, not disagreement — J-coupling makes the methylene decay echo-spacing-dependent (Henkelman 1992) and the six-line chemical-shift spectrum (Hamilton 2011) makes chemical-shift-encoded modelling the right tool. The row is a 90/10 fat/water mixture declared a **misspecification row**: the pure-exponential model it is scored under is known to be wrong for it, and even under that model the 10 % water fraction is not estimable at the reference acquisition (relative SD ~60 %). (2) *Fat content on every entry as two numbers*: `lipid_mass_fraction` (chemical lipid, Woodard & White, PRIMARY) and `pdff` (MR proton-density fat fraction), kept distinct because white matter is 18 % lipid by mass and 0 % PDFF (membrane lipid is invisible at TE ≥ 1 ms); marrow PDFF 33 ± 8 % is PRIMARY (Le Ster 2016 Table 1), the lean-organ PDFFs are RECALLED ranges and the next verification target; hepatic steatosis (PDFF > 5.56 %, Szczepaniak 2005; grades ~6.4/17.4/22.1 %, Tang 2013) and the other fat pathologies are logged under the pathology axis. (3) *Four primaries applied*: Woodard & White 1986 Table I moved the water-content column to PRIMARY for 15 entries (cord and medulla are THEORY inferences from it); de Bazelaire 2004 Tables 1–2 moved the abdominal and pelvic T1/T2 columns at both fields to PRIMARY and filled the 1.5 T columns for spleen, kidney cortex/medulla, pancreas, prostate and liver that were RECALLED; Gold 2004 replaced the Han 2003 abstract for fat and marrow; Le Ster 2016 Table 1 confirmed the marrow row. Component-value counts: 7 PRIMARY / 31 SECONDARY / 1 TERTIARY / 6 RECALLED / 11 THEORY. JSON schema 2.1; document regenerated (774 lines); `results/threshold_tissues.md` regenerated with the adipose reading; 70 tests pass.

**Licenses and remote.** Apache-2.0 for the code (`LICENSE`, full text taken from an installed Apache-licensed package's dist-info plus the standard appendix, copyright 2026 Eric Diaz), CC BY 4.0 for the documents, dictionary and results (`LICENSE-DOCS`); `README`, `CITATION.cff` (version charter-v0.12) and `pyproject.toml` updated. The GitHub mirror cannot be created from the cloud session (no credentials); `README` §Remote gives the two-line `gh repo create … --push` recipe to run once from the Mac; charter §8 records both decisions.

**Register.** Header version label corrected to 0.8 (it had stayed at 0.4 while the history ran to v0.7 — the same defect the charter had at v0.9). E3 updated to v2.1 and now carries the *future research project*: in vivo multi-component T2 relaxometry of liver, spleen, kidney and pancreas acquired with IVIM in the same session, parked by Eric's decision so that it is neither lost nor pursued by accident. E1 notes the release preconditions now met (licenses, mirror) and the ones that are not (E2, a scored estimator family). B1/D3 notes carry the printed constant next to the relative form. No status changes.

**`literature-requests.md`.** Eric's `literature-requests-updated.md` (a retrieval-status column, every row **Retrieved**) adopted as the living copy in `docs/project/` and the project; the Lanczos row reads "on hand (local folder)".

**Not done, deliberately.** The rest of the dictionary verification pass (Stanisz 2005 from the publisher PDF, Whittall 1997, MacKay 1994, Saab 1999, Araujo 2014, Sabouri 2017 ×2, MacMillan 2011, Labadie 2014, Prasloski 2012, Du 2012, Bouhrara 2015, and the lean-organ PDFF ranges) is the next session's first task, with every PDF now on hand; the pathology axis follows it, steatotic liver first.

---

## 2026-09-07 (evening) — charter v0.10 → v0.11 · gaps register v0.6 → v0.7 · bibliography v0.1 → v0.2 · tissue dictionary v1 → v2 · `literature-requests.md` opened

**Trigger:** Eric's page images of Lanczos pp. 272–279; his request for a deep, cited tissue dictionary with PD, T1 and diffusion and for liver, spleen and kidney; his decisions on the open questions (E1 list kept with flag; adjacency notes kept; two-level threshold framing accepted); links wanted for Varah 1985 and Istratov & Vyvenko 1999.

**§1.2 / §4 — Lanczos closed against the primary.** The grid is Δx = 0.05 h from x = 0 (the NIST grid); the table is two decimals "accurate to ½ unit"; the three-exponential Prony attempt is "redundant within the errors"; the coefficients are 2.202 / 4.45 / 0.305 / 1.58 (eq. 4-23.16); the stated agreement is 0.006 max at k = 5 and 0.0026 RMS; the generating law is eq. 4-23.17. The harness reproduces the data table, both sets of grouped Prony sums, his fitted table to 0.002 and both agreement figures; the `provisional_pending_primary` marker is removed and six Lanczos-specific tests lock it. The Δt = 0.1 hypothesis is dead. Varah's tech report does not quote the coefficients; the journal version remains a citation check only.

**§1.2 — SNR convention resolved in principle.** Amplitude SNR by derivation (singular value × signal norm vs σ); arccosh(SNR²) exact, 2 ln SNR the limit; the power reading would give 4.2 at SNR 10³ against the 2.0 that Steinbeck & Chmelka 2005 quote from Istratov & Vyvenko. The paper is still to be read for the citation (DOI 10.1063/1.1149581, `literature-requests.md` #1).

**Tissue dictionary v2 (`mexp/tissue_data.py`).** Restructured as data-as-code with a generated JSON (`data/tissue_dictionary.json`) and a generated document (`docs/tissue-dictionary.md`, 690 lines): per entry, water content / relative PD, T1 and T2 at 1.5 and 3 T, ADC; components per modality (T2; T1; IVIM / tensor / standard-model diffusion) with fraction, value, range, status and source per component; the clinical functional and a typical acquisition; a theory block (physical pools, exchange regime, apparent vs clinically resolvable counts). 18 entries: liver, spleen, kidney cortex and medulla, pancreas, vertebral marrow, arterial blood and CSF added. Values from the literature research of this session: Stanisz 2005 Table 1 (via mirror), de Bazelaire 2004, Gold/Han 2004, MacKay 1994, Whittall 1997, MacMillan 2007/2011, Saab 1999 (four muscle components), Reiter 2009 (cartilage, PRIMARY), Horch 2010 (bone, PRIMARY), Labadie 2014, Le Ster 2016, Luciani 2008 (liver IVIM, PRIMARY), Li 2017, Yamada 1999, van Baalen 2017, Filli 2015, Serafin 2024, Mayer 2021, McGill 2015, Mazzoli 2021, Oros-Peusquens 2019, Jelescu & Budde 2017. Status count of component values: 7 PRIMARY, 30 SECONDARY, 1 TERTIARY, 6 RECALLED, 10 THEORY. The abdominal T2 splits are THEORY because no in vivo multi-component T2 study of liver, spleen, kidney or pancreas was found (Bernardino 1983 ex vivo and Tasbihi 2024 rat are the nearest). Muscle changed materially from v1: Saab's abstract gives < 5 / 21 / 39 / 114 ms at 11 / 28 / 46 / 5 %, not the 4 / 33 / 130 ms recalled. Framework stated: 4–7 pools → 2–3 apparent T2 components → 1–2 resolvable, via exchange, partial volume, visibility window and intra-pool dispersion.

**CRLB — inversion made robust.** `mexp.crlb` now inverts the FIM in relative coordinates; the raw pseudo-inverse had truncated a near-singular direction (the unpinned 2000 ms component) to *zero* variance in one dictionary row. Previously reported reference-configuration numbers are byte-identical before and after the fix (checked by diff of `results/threshold_evaluation.md`). Singular directions are now reported as infinite variance with a `singular` flag.

**§1.2 tissue-level paragraph rewritten** for v2 (per-tissue reading incl. muscle's collapse from four to an unestimable three components at clinical SNR, the marginal abdominal splits, and K = 1 as the correct answer for myocardium, blood, CSF). **§4** — dictionary bullet updated; Lanczos provenance paragraphs replaced by a closed statement. **§7** — open flags reduced to Istratov & Vyvenko; `literature/` folder rule. **§8** — five rows (Lanczos closed; SNR convention; dictionary v2 + CRLB fix; Eric's decisions; plus the existing v0.9 row). **§10** — step 3 closed; step 8 expanded (verification from `literature/`, replacing THEORY rows, the pathology axis, organs and fields to add); 64 tests.

**Bibliography v0.2** — DOIs added and confirmed for every foundations/noise-track row and the dictionary sources; corrections: Bertero Part II authors (Boccacci, not Brianzi); Sabouri's second paper is JMRI 46:861, not MRM 77:2038; Vaidyanathan & Pal author order; Bates & Watts page range includes the discussion. **`literature-requests.md`** opened: 57 items with full citations and DOI links in three priorities, with a file-name convention for `literature/`.

**Gaps register v0.7** — E3 updated to v2 and its confidence raised to 0.7 (the abdominal measurements themselves appear missing); A1 note corrected for the Bertero authorship.

**Repository** — `literature/` (git-ignored) with README; `data/` for the JSON release; `requirements.txt` pinned; `CITATION.cff`; charter version tags; two-remote note. License still to be chosen (see the session's recommendation).

---

## 2026-09-07 (later still) — charter v0.9 → v0.10 · gaps register v0.5 → v0.6 · bibliography v0.1 opened

**Trigger:** Eric's review of the session output — the threshold's amplitude vector must not be "myelin or nothing"; the code needs a home; the living documents need to be in one place with history. Housekeeping: v0.9's header still read "Version: 0.8" (only the summary had been bumped); fixed here.

**§1.2 — tissue-level threshold.** A tissue and organ dictionary (`mexp/tissues.py`) is now a harness component: per (tissue, modality, field) the component fractions and time constants with literature ranges, the clinical functional and its threshold, a typical acquisition, sources, and a provenance status. Thirteen entries seeded, all `RECALLED` (from memory of the named sources; the verification pass is §10 step 8). The §1.2 clauses are evaluated over every entry (`scripts/phase0_threshold_tissues.py`, `results/threshold_tissues.md`). First reading: under a free K = 3 model no myelin-type entry is estimable at first-echo SNR 100 — the unpinned 2000 ms component roughly doubles the MWF bound; with it fixed or dropped the white-matter MWF bound is ±0.030 at SNR 100 and ±0.010 at SNR 300, myelin T2 itself at 57 % relative SD (functional better determined than parameters); minor components under ~5 % fail; well-separated two-component tissues (prostate) are estimable. The "K = 2 comfortably, K = 3 marginal" sentence is restated per tissue class. The kernel-level two-vector clauses of v0.9 are kept as the tissue-agnostic statement.

**§4 Phase 0** — dictionary bullet added; the synthetic generator's truth set now includes every dictionary entry with its functional. **§6.1** — ground-truth axis gains the "literature-derived tissue composition" level. **§7** — `bibliography.md` opened as the one G1 list (v0.1: every source cited so far, each with a `CHECKED-PRIMARY` / `CHECKED-ABSTRACT` / `BIBLIO-ONLY` / `RECALLED` status — most are `RECALLED`, which is the honest state); new practice: the git repository in the connected folder is the code's home and mirrors the living documents under `docs/project/`, with the higher version number authoritative on disagreement. **§8** — rows for the dictionary and the repository. **§10** — step 2's "no home" resolved; step 8 added (dictionary verification pass); old step 8 → 9.

**Gaps register v0.6** — E3 added (open, cited, machine-readable tissue-composition table; `UNVERIFIED`, 0.6).

**Harness** — `mexp/tissues.py`, `mexp.crlb.mass_below_gradient` / `functional_sd` (delta-method bound on a functional such as MWF), `tests/test_tissues.py`, `scripts/phase0_threshold_tissues.py`; 60 tests. Repository created at `ExponentialParameterEstimation/` with `mexp-harness/` and `docs/project/`.

**Still open (for Eric):** Lanczos book check (four fields); Istratov & Vyvenko convention; whether the v0.9/v0.10 threshold wording is accepted; the v0.7 changelog's items #4 (E1 list vs the sweep's bibliography — now checkable against `bibliography.md`) and #5 (keep the register's adjacency notes — kept). The Bouhrara citation (#3) is confirmed by title and PMC ID.

---

## 2026-09-07 (later) — charter v0.8 → v0.9 · gaps register v0.4 → v0.5

**Trigger:** the harness session re-ran its Phase 0 work against charter v0.7/v0.8 (it had worked from v0.6 the first time), built the CRLB module that v0.8 §10 named as the next item, and evaluated the §1.2 falsifiable threshold. Findings in `claude/phase0-findings.md` (revision 2); harness in `mexp-harness.zip` (55 tests); `results/threshold_evaluation.md` inside it carries every number below.

**§1.2 — Threshold evaluated; amplitude vectors written into the clauses.** At the reference configuration (32 × 10 ms, T2 support 10–300 ms, first-echo SNR 100, Rician) none of the three v0.8 clauses fires: K = 3 at adjacent ratio 3 has 60–155% relative SD on the decay constants (best all-three case 70 / 103 / 30%), so the upward clause misses by ×3–15; K = 2 at ratio 4 has 5–14% at equal amplitudes, so the downward clause does not fire — but at a 20/80 split the short constant reaches 28–41% and it would have, for 4 of 12 placement × amplitude cases. Since the outcome depends on the amplitude vector more than on placement, on Rician vs Gaussian, or on σ known vs jointly estimated (all < 2%), the clauses now name two vectors (equal; 20/80 with the minor component shortest) and the downward clause fires on the equal-amplitude case only. The window-rule clause holds: six spacings at N = 8–32 over a fixed window move the count by at most one. Outcome paragraph added under the clauses; "CRLB module (next Phase 0 item)" → "built".

**§3.2 — Bound-level probe recorded.** Under the correct single-coil Rician likelihood at 32 echoes, the joint-σ CRLB is within 2% of the σ-known CRLB for the decay constants (SNR 100 and 30; with and without a 2000 ms component). The cost of the σ / long-T degeneracy, if it is large, lives on the misspecification, free-offset, χ-pipeline and external-σ rungs — which is where §10 step 6 should aim. One paragraph; the novelty claim is unchanged.

**§4 Phase 0.** Conditioning bullet: the Epstein–Schotland / McWhirter–Pike check qualified as a check on count and decay rate, not index by index (σ₁/σ₀ ≈ 0.33 numerically for every covering-window Γ ≥ 100 against 0.48–0.78 from uniform Mellin quantization; f-weighting moves the count by 1–3). Lanczos bullet restated for the restructured test — (a) applied to the harness's own least-squares fit computed inside the test through the kernel (so `mexp/` still has no estimator), quoted coefficients checked separately, (b) indistinguishability stated via the expected excess χ² (≤ 5.5 < 5.99 for σ ≥ 8.8 × 10⁻⁴) and the K = 3 CRLB — and marked provisional with a pytest marker. Provenance flag narrowed further: the t = 0 deviation is grid-independent, so only the two-decimal reading is consistent with the quoted pair; the pair sits closer to the Δt = 0.1 long-window optima (2.1885, 4.5035; 0.3236, 1.5963 on [0, 2.3]; 2.2156, 4.4682; 0.2961, 1.5247 on [0, 3.2]) than to the NIST-grid optimum; Varah's 1982 tech report does not quote the coefficients, so the 1985 journal version needs checking before Varah is cited for them. CRLB bullet marked built. Foundations table given a Phase 0 status line (encoded: M–P/BBP/I–V, E&S, Ostrowsky, Transtrum, Rife–Boorstyn; not yet: Bates–Watts).

**§8** — two rows: the threshold outcome with the amplitude-vector decision and its reversal condition; the closed-form-is-asymptotic-in-index record with the Lanczos grid and Varah notes.

**§10** — step 2: 55 tests; step 3: test restructured, book check given four named fields (Δt, points, decimals, claimed agreement); step 5 marked done with `crlb_optimal` still open; step 6 carries the §3.2 probe as its aiming note.

**Gaps register v0.5** — Phase 0 numerical notes on A1 (placement at fixed window moves the count by ≤ 1, so the design question must be posed in CRLB units), A3 (the joint block exists numerically; item 2's cost is small in the clean regime, aim items 3–4 at the misspecified/offset/pipeline rungs) and D1 (the finite-window count is measured; the linear-algebraic half of the austere-budget impossibility is on record). No status changes.

**Harness** — `mexp/crlb.py` added (Gaussian real/complex, Rician σ-known and joint; Rife–Boorstyn and sloppy-spectrum tests); `conditioning.r_min / k_max / ostrowsky_spacing / mellin_closed_form_ratios` added with both constant conventions; Lanczos test restructured; k-space-first guard refined to forbid noise *generation*, not the naming of a likelihood; `scripts/phase0_threshold.py` added. Still open: the harness has no home outside the conversation.

---

## 2026-09-07 — charter v0.7 → v0.8 · gaps register v0.3 → v0.4

**Trigger:** the harness session's `claude/phase0-findings.md` and `claude/phase0-harness-status.md` (written 2026-09-06/07) asked for §1.2 and the §4 Lanczos text to be re-checked against measured Phase 0 numbers, and found the project still holding charter v0.6 because v0.7 had been delivered as a file but not written back. Both fixed here.

**§1.1** — one sentence noting that a_k > 0 is the T2 statement and that the harness generalizes the amplitude field (positive · real · complex) for IR-magnitude and complex T2\*; the §2 reuse claims rest on that.

**§1.2 — Lanczos, made exact.** The v0.7 flag ("the grid, the 0.001 claim, and the quoted coefficients are mutually inconsistent") is resolved by the findings: Lanczos's table was given to two decimals; his own two-term coefficients reproduce that table at 23 of 24 points (max deviation 0.0064 from f); the optimal two-exponential approximant, 2.0688·e^(−4.6396t) + 0.4440·e^(−1.8725t), agrees to 8.8 × 10⁻⁴. So the "better than 0.001" in the pass-1 brief belongs to the optimal approximant, not to Lanczos's own fit. Added the oracle-detector separation figure (SNR ≳ 3.6 × 10³). Grid and three-exponential coefficients are now locked from NIST StRD (which cites Lanczos pp. 272–280); only Lanczos's own two-term coefficients remain a book check, and no test depends on them.

**§1.2 — Rank, reconciled to measurement.** Three changes. (1) The resolvable-ratio closed form is written in the exact arccosh form the harness implements, δ = exp(π²/arccosh(SNR²)) — 3.7 / 2.7 / 2.0 at SNR 30 / 100 / 1000 — which also settles the O(ln 2) convention question v0.7 flagged; the finite-window cost (factor ≈ 1.1–1.2) is stated from the harness. (2) The count expression now carries the acquisition window: N_inf ≈ (ln γ_eff/π²)·arccosh(SNR²) + 1, γ_eff = min(T_max/T_min, t_max/t_min), ±1.5. v0.7's K_max ≈ 1 + 2·ln SNR·ln Γ/π² used T-range only and would overstate the rank by 2–5 for realistic trains; the harness showed the window binds and that sampling density inside it barely matters. Measured counts for 32 × 10 ms: 3, 4, 5, 6 at SNR 30, 100, 300, 1000. (3) Units. "2–3" is a count of *components* (2K rule: K components need ≥ 2K informative directions), while the numerical rank is 3–6; v0.7 did not say which, and its falsifiable threshold ("revise upward if the SVD count is ≥ 4") conflated the two and would already have fired at the measured 4. The threshold is moved to the CRLB module (upward if K = 3 at ratio 3 is < 10% RSD at SNR 100; downward if K = 2 at ratio 4 exceeds 25%), plus a window-rule test.

**§2.3** — "few samples" bullet refined: the window binds, not the sample count within it; sparse designs spanning the same window cost mainly the √N energy gain. This strengthens the case for the sparse-ruler study.

**§4 Phase 0** — conditioning bullet re-axed (echo count and window replace "echo spacing"), marked done for T2 with the Picard diagnostic noted; Lanczos correctness test restated per the findings' recommended wording and marked passing (NIST table reproduced to 4.8 × 10⁻¹³; optimal approximant verified as a stationary point; oracle-SNR figure); provenance flag narrowed to the one open book check; the NIST Lanczos3 flip-side example added. k-space-first rule annotated as mechanically enforced by `tests/test_kspace_first_rule.py`.

**§7** — provenance flags updated to the two that remain open. New working practice: living documents are written back in place the same session, with the changelog appended, and companion findings docs that contradict the charter trigger a reconciliation rather than being left to disagree. Delivering a file is not saving it.

**§8** — two entries dated 2026-09-07: the reconciliation (with reversal conditions on the CRLB module and the few-point kernels), and the harness's SNR definition (S_ref/σ₁ at the R = 1 single-coil reconstruction of the same k-space noise; per-sample and energy conventions reported alongside), which the harness-status doc asked to have recorded.

**§10** — steps 2, 3 and the conditioning map marked done with what is open on each (harness code has no home yet; Lanczos book check); the CRLB module is now the named next item, followed by the joint (θ, σ) FIM, the simulator, the EPG path, and `crlb_optimal`.

**Gaps register v0.4** — formula in the B1 note and the D3 note aligned with §1.2's harness form. No status changes; the findings do not bear on any entry's status (A3 is next in the harness queue but not started).

**Harness-status doc** — open items 1 and 4 marked done (v0.7/v0.8 saved; cross-reference fixes landed in v0.7).

**Items still flagged for you:** the Lanczos p. 276 book check; Istratov & Vyvenko's printed SNR convention vs. the arccosh form; the identity of "the 2014 biexponential Rician work" as Bouhrara et al.; the E1 nearest-neighbour list vs. the sweep's bibliography; the register's adjacency notes. And one decision only you can make: where the harness code lives.

---

## 2026-09-06 — charter v0.6 → v0.7 · gaps register v0.2 → v0.3

**Trigger:** three corrections and one set of omissions established by the pass-1 foundations sweep; one claim upheld. *(Note added 2026-09-07: v0.7 was delivered to the conversation but not written to the project until 2026-09-07; the harness session in between worked from v0.6. Its Lanczos and §1.2 findings superseded parts of this entry — see the v0.8 entry above.)*

## Charter v0.6 → v0.7

**§1.2 — Lanczos example (correction 1).** The example is now stated correctly: a *three*-exponential sum, f(t) = 0.0951·e^(−t) + 0.8607·e^(−3t) + 1.5576·e^(−5t), matched to better than 0.001 in amplitude over 24 sampled points by the *two*-exponential sum 2.202·e^(−4.45t) + 0.305·e^(−1.58t). v0.6's §1 did not actually contain the Lanczos text (it lived only in §4 Phase 0 and §10); the correct statement was added to §1.2 as the canonical illustration of the ill-posedness, because that is where a reader first needs it. The old wording is quoted and marked as corrected. *Why:* the mis-description ("two very different 3-exponential sums") missed the example's point — that the *number* of components is not identifiable — and would have locked in a correctness test checking the wrong property.

**§1.2 — Effective numerical rank (correction 2).** The constant "roughly 2–3" is replaced by the dependence: Istratov & Vyvenko's closed form for the minimum resolvable ratio of adjacent time constants, R_min(SNR) = exp(π²/(2 ln SNR)), derived from the McWhirter–Pike / Bertero Mellin-domain cutoff ω_max ≈ (2/π)·ln SNR; Bertero, Boccacci & Pike's result that the recoverable count grows only as ln SNR and improves with restricted support; and a heuristic combined estimate K_max ≈ 1 + 2·ln SNR·ln Γ / π² over a T-range of ratio Γ. Worked numbers at SNR 30/100/1000 are given. The ~2–3 figure at clinical SNR is retained, but derived as a consequence at a stated operating point (first-echo SNR 50–200, T2 support 10–300 ms), with the separability-vs-useful-precision distinction and the finite-window effect explaining the step from the closed form's 3.5–4.5 down to 2–3. A **falsifiable threshold** is attached, tied to Phase 0's numerical SVD at a reference clinical configuration (32 echoes, 10 ms spacing, T2 support 10–300 ms, first-echo SNR 100, Rician), with explicit upward and downward revision conditions. "The single most important quantity" is now the map, not the number.

**§1.3 — Sloppy models.** One sentence added placing Transtrum, Machta & Sethna's sloppy-model geometry as the fitting-side statement of the same ill-conditioning (the sum of exponentials is their canonical sloppy model).

**§3.2 — Joint (θ, σ) novelty claim (correction 3).** The sentence "to my knowledge has not been done systematically for multi-exponential relaxometry" is replaced by a "what is and is not new" paragraph naming the adjacent prior art: Benedict & Soong (1967) and Sijbers & den Dekker (2004) on joint single-amplitude/σ estimation; Karlsen et al. (1999) and Bouhrara et al. (MRM 73:352–366, online 2014) on Rician-correct CRLBs for decay models; Milford et al. (2015) on the free-offset practice. The claim is narrowed to the multi-exponential joint FIM block inverse (K ≥ 2 Schur complement of σ) and the analytic σ / long-T degeneracy manifold. §3.7's first bullet and §10 step 4 were narrowed to match.

**§4 Phase 0 — Correctness test and foundations table.** The Lanczos correctness test is restated with explicit pass criteria and a **provenance flag**: coefficients verified via NIST StRD Lanczos1–3, Istratov & Vyvenko (1999), and Varah (1985), not the primary; Lanczos (1956) must be checked for grid, precision, and coefficients before the test locks. A new table, **"Foundational results the harness must encode"**, adds the results the sweep found missing entirely — Transtrum/Machta/Sethna sloppy models, Bates–Watts curvature decomposition, Ostrowsky et al. exponential sampling, Epstein–Schotland closed-form singular values, Rife–Boorstyn single-tone CRLB as the contrasting sinusoid case — plus McWhirter–Pike / Bertero et al. / Istratov & Vyvenko as the source of §1.2. Each row states what it establishes and where it plugs in. The Phase 0 deliverable now records that the no-open-benchmark claim was upheld, pointing to E1 for the evidence.

**§7 — Provenance discipline.** New working practice: any number that becomes a harness correctness test or a charter threshold is traced to its primary before locking; secondary confirmation suffices to write it down with a flag, not to lock on it.

**§8 — Decision log.** Five entries dated 2026-09-06: Lanczos corrected; rank restated with threshold; joint-FIM claim narrowed (A3 → PARTIAL); foundations added; no-benchmark claim upheld (E1 stays UNVERIFIED, leaning CONFIRMED). Each carries the options considered, reasoning, and reversal condition.

**§10 — Next steps.** Step 1 marks pass 1 complete and names the remaining passes. Step 3 gates the Lanczos test on the primary check. Step 4 carries the narrowed scope. Step 5 records the pass-1 register outcome. New step 6: build the Phase 0 conditioning map and evaluate the §1.2 threshold.

**Housekeeping (not decisions):** "Six families" → "Seven families" in §4 Phase 1 (family 7 was added in v0.3 but the header was never updated); §6.1 method-family axis cross-reference corrected from "six families of §3" to "seven families of §4". Nothing else in §2, §3.1, §3.3–3.6, §5, §6, or §9 was altered.

## Gaps register v0.2 → v0.3

**A3 → `PARTIAL`.** The original claim is kept verbatim for the calibration record. The prior art is listed under "what is occupied" with full citations, and the open sub-question is enumerated in four parts (K ≥ 2 joint FIM with σ nuisance; block inverse / Schur complement; analytic degeneracy manifold with identifiability conditions and the free-offset estimator located on it; the K ≥ 2 value-of-noise-knowledge ladder). Original confidence 0.8 retained as the number to be scored; a separate 0.7 stated for the narrowed sub-question. Reversal condition to `OCCUPIED` stated.

**E1 stays `UNVERIFIED (leaning CONFIRMED)`.** The upholding evidence is now cited: the nearest neighbours (NIST StRD Lanczos sets; ISMRM/NIST system phantom; the 2020 T1 reproducibility challenge; diffusion-specific challenges; per-group simulators and toolboxes) each with the axis on which it falls short. "Why not CONFIRMED" is stated — pass 1 did not cover the method families or the four disciplines' test-problem practices — along with what would move it to `OCCUPIED`.

**§0.2 — Lean annotations defined.** "UNVERIFIED (leaning CONFIRMED)" is now a defined construct: an annotation recording evidence from a nearby pass, not a status; it does not lift the exploratory-scoping restriction.

**Adjacency notes, statuses unchanged.** Short notes on A1, B1, B5, B6, C1–C3, D1, and D3 recording prior art the *foundations reading* bears on (Bertero Part II and Ostrowsky for A1; the R_min baseline and Epstein–Schotland for B1; Transtrum et al. for B5; Bates–Watts for B6; Rife–Boorstyn scaling for the Fourier-side transfers; Bertero/Istratov–Vyvenko impossibility results for D1; the §1.2 map for D3). Each is explicitly labelled as derived from the foundations reading, not from a check of the entry, and no status was changed. These are positioning constraints for future work, not verdicts.

**§7 — Calibration ledger opened**, with A3 as its first row, and the rule that a `PARTIAL` counts as a half-miss.

**§0.1, §6** — one-sentence updates noting the first instance of the process working (A3) and that pass 1 alone does not warrant re-ranking.

---

## Items flagged for you to check

1. **Lanczos primary — now more than a formality.** While verifying the example I fit the two-exponential model to the three-exponential sum numerically. On the NIST 24-point grid (t = 0, 0.05, …, 1.15) the least-squares-optimal two-exponential is 2.069·e^(−4.64t) + 0.444·e^(−1.87t), max deviation 0.00088 — so "better than 0.001 over 24 points" is achievable and the example's point stands. But the quoted coefficients 2.202·e^(−4.45t) + 0.305·e^(−1.58t) deviate by 0.0064 at t = 0 on that grid; they match the optimum on a Δt = 0.1 grid over 0–2.3 (2.189·e^(−4.50t) + 0.324·e^(−1.60t)), where the best achievable agreement is ≈ 0.0024. The grid, the 0.001 claim, and the quoted coefficients cannot all be right together as stated. I kept the quoted numbers in the charter (they are what the sweep established), recorded the inconsistency in the §4 provenance flag and the §8 row, and made the correctness test's pass criterion apply to the harness's own best fit on the primary's grid, with the quoted coefficients checked separately. *Applied Analysis* (1956), ch. IV needs to settle the grid, the stated precision, and the coefficients as Lanczos reports them.
2. **R_min constant convention.** I could not reach the Istratov & Vyvenko primary or Bertero 1982 from here (paywalled / blocked). The form exp(π²/(2 ln SNR)) follows from the Mellin cutoff ω_max ≈ (2/π) ln SNR, which I did confirm independently (it appears as y_max ≈ (2/π)·m·ln 10 for m decimal digits in a Laplace-inversion paper), but whether Istratov & Vyvenko write SNR as an amplitude or power ratio, and whether they carry the O(ln 2) normalization term, should be pinned before the resolution test locks. Flagged in §1.2 the same way as the Lanczos coefficients.
3. **"The 2014 biexponential Rician work."** I identified this as Bouhrara, Reiter, Celik, Bonny, Lukas, Fishbein & Spencer, MRM 73(1):352–366, online February 2014 (Crossref-confirmed). If the sweep meant a different paper, swap the citation in charter §3.2/§3.7/§8/§10 and register A3.
4. **E1 upholding evidence.** The nearest-neighbour list is my reconstruction of what a no-open-benchmark finding rests on; match it against the sweep's actual bibliography entries and prune anything the sweep did not look at.
5. **Adjacency notes in the register.** These were derived from the foundations list you supplied, not from sweep verdicts. If you would rather the register carry only sweep-verified annotations, delete them — no status depends on them.

Citations verified today against a bibliographic source: NIST StRD Lanczos1 (model, 24 observations, Lanczos 1956 provenance); Bouhrara et al. 2014; Milford et al. 2015; Benedict & Soong 1967. Cited from knowledge without a live check today: Sijbers & den Dekker 2004, Karlsen et al. 1999, Epstein & Schotland 2008, Ostrowsky et al. 1981, Rife & Boorstyn 1974, Bates & Watts 1980/1988, Transtrum et al. 2010/2011/2015, McWhirter & Pike 1978, Bertero et al. 1982/1984, Varah 1985.
